# Copy to .env (local) or set as an environment variable in Cloudflare Pages.
# Leave unset and the site ships with zero analytics scripts — no broken trackers.

# GA4 measurement ID, e.g. G-XXXXXXXXXX
PUBLIC_GA4_ID=
